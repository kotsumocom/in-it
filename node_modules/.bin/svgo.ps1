#!/usr/bin/env pwsh
$basedir=Split-Path $MyInvocation.MyCommand.Definition -Parent

$exe=""
if ($PSVersionTable.PSVersion -lt "6.0" -or $IsWindows) {
  $exe=".exe"
}
$ret=0
if (Test-Path "$basedir/node$exe") {
  if ($MyInvocation.ExpectingInput) {
    $input | & "$basedir/node$exe"  "$basedir/../.deno/svgo@3.3.2/node_modules/svgo/bin/svgo" $args
  } else {
    & "$basedir/node$exe"  "$basedir/../.deno/svgo@3.3.2/node_modules/svgo/bin/svgo" $args
  }
  $ret=$LASTEXITCODE
} else {
  if ($MyInvocation.ExpectingInput) {
    $input | & "node$exe"  "$basedir/../.deno/svgo@3.3.2/node_modules/svgo/bin/svgo" $args
  } else {
    & "node$exe"  "$basedir/../.deno/svgo@3.3.2/node_modules/svgo/bin/svgo" $args
  }
  $ret=$LASTEXITCODE
}
exit $ret
